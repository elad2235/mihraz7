pipeline {
    agent { docker 'elad2235/mihraz7:env' } 
    stages {
			stage('Setup Dependency') {
				steps {
					    withEnv(["HOME=${env.WORKSPACE}"]) {
					      sh "pip3 install -r requirements.txt --user"
						
    					}
				}


        }
			stage('Run Tests') {
				steps {
					    withEnv(["HOME=${env.WORKSPACE}"]) {
					      sh "python3 -m xmlrunner discover mihraz7/ -o ./test-reports/junit/"
						  sh "python3 -m pyflakes mihraz7/"
						  sh "python3 -m flake8 --ignore W191,E501 --output-file flake8.txt"
    					}
						
				}
				post {
                always {
					withEnv(["HOME=${env.WORKSPACE}"]) {
                    	junit 'test-reports/junit/*.xml'
						sh "flake8_junit flake8.txt ./test-reports/flake8_junit.xml"
						junit 'test-reports/flake8_junit.xml'
					}
                }
            }
			}
	}


	post {
		always {
            echo 'Finishing up'
            deleteDir() /* clean up our workspace */
        }
    	failure {
        	 mail to: 'eladpe@ac.sce.ac.il',
             subject: "Failed Pipeline: ${currentBuild.fullDisplayName}",
             body: "Something is wrong with ${env.BUILD_URL} "
    }
}


}
